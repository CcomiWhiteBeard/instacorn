GIT BASH 설치!



https://git-scm.com/

그대로 설치

cmd 실행

cd\
cd mtest
cd workpython

git remote -v
git status

git init
git config --global user.email "본인메일.com"
git config --global user.name "본인이름"
dir
cls

깃허브 접속해서 repository project
git init
git add *.*
git commit -m "test 05-20"
git remote
git remote add origin "https://github.com/BetaZelda/project.git"
git remote
git branch
git push origin master


git remote -v	==>Git주소 표시됨

